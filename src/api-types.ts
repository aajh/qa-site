export interface Question {
    id: string,
    author: string,
    title: string,
    body: string,
    created: string,
}

export interface QuestionSummary {
    id: string,
    author: string,
    title: string,
    created: string,
}
